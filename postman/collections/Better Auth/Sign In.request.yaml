$kind: http-request
url: "{{baseUrl}}/api/auth/sign-in/email"
method: POST
headers:
  Origin: "{{baseUrl}}"
body:
  type: json
  content: |-
    {
      "email": "{{betterAuthEmail}}",
      "password": "{{betterAuthPassword}}"
    }
order: 2000000000000
